{
  "name": "argframe",
  "displayName": "ArgFrame",
  "description": "Convert YAML files representing argumentation frameworks into HTML reports using Geist",
  "publisher": "CIRSS",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/xai-ca/argframe-vscode.git"
  },
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Visualization",
    "Linters"
  ],
  "keywords": [
    "Formal Argumentation",
    "Argumentation Framework",
    "Visualization"
  ],
  "activationEvents": [
    "onLanguage:yaml"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "argFrame.generateReport",
        "title": "Generate HTML Report",
        "icon": "$(preview)"
      },
      {
        "command": "argFrame.refreshPythonInterpreter",
        "title": "Refresh Python Interpreter",
        "category": "ArgFrame"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "argFrame.generateReport",
          "when": "resourceExtname == .yaml || resourceExtname == .yml",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "ArgFrame",
      "properties": {
        "argFrame.pythonInterpreter": {
          "type": "string",
          "default": "",
          "description": "Path to Python interpreter with geist package installed (required)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "@types/js-yaml": "^4.0.5",
    "typescript": "^4.9.4"
  }
} 